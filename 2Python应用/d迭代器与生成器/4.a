#手动遍历迭代器
#大多数情况下，会使用for循环语句用来遍历一个可迭代对象。但是，偶尔需要对迭代
#做更加精确的控制。
>>>iters = [1,2,3]
>>>it = iter(items)
>>>next(it)
1
>>>next(it)
2
>>>next(it)
3

#__next__()方法 next()函数

with open('/etc/passwd') as f:
	while True:
		line = next(f) 
		if line is None:
			break
		print(line,end'')

